# Archived Results

Files under `invalidated_2026-06-13/` are stale model parameters generated from
pre-fix data or calibration protocols. They are retained for provenance only.

The only active calibration artifact is `results/calibrated_params.json`.
